## Contains notes, must be either changed or deleted before hand-in!

1. Must make "Location" in registration-form a dropdown list with hardcoded cities, like tags for without multiple tag. Maybe add cities ENUM.
2. Needs access to several endpoints from homepage like place Get("/attractions") probably, waiting on those endpoints being finished and merged in.
3. Css needs a class for alligned input fields. Please write down more CSS requests here. 
